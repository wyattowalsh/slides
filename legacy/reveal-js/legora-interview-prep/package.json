{
	"name": "legora-interview-prep-deck",
	"private": true,
	"version": "0.1.0",
	"type": "module",
	"scripts": {
		"dev": "vite --host 127.0.0.1",
		"build": "vite build",
		"preview": "vite preview --host 127.0.0.1",
		"validate:data": "node scripts/validate-data.mjs",
		"check:sources": "node scripts/check-sources.mjs",
		"test:deck": "node scripts/test-deck.mjs",
		"test:visual": "node scripts/visual-check.mjs",
		"verify": "npm run validate:data && npm run build && npm run check:sources && npm run test:deck && npm run test:visual"
	},
	"dependencies": {
		"reveal.js": "^6.0.1",
		"reveal.js-menu": "^2.1.0"
	},
	"devDependencies": {
		"puppeteer": "^24.43.0",
		"vite": "^8.0.3"
	}
}

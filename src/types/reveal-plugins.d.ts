declare module "reveal.js-copycode" {
	import type { RevealPlugin, RevealPluginFactory } from "reveal.js";

	const plugin: RevealPlugin | RevealPluginFactory;
	export default plugin;
}

declare module "reveal.js-appearance" {
	import type { RevealPlugin, RevealPluginFactory } from "reveal.js";

	const plugin: RevealPlugin | RevealPluginFactory;
	export default plugin;
}

declare module "reveal.js-mermaid-plugin" {
	import type { RevealPlugin, RevealPluginFactory } from "reveal.js";

	const plugin: RevealPlugin | RevealPluginFactory;
	export default plugin;
}

declare module "reveal.js-simplemenu" {
	import type { RevealPlugin, RevealPluginFactory } from "reveal.js";

	const plugin: RevealPlugin | RevealPluginFactory;
	export default plugin;
}

declare module "reveal.js-verticator" {
	import type { RevealPlugin, RevealPluginFactory } from "reveal.js";

	const plugin: RevealPlugin | RevealPluginFactory;
	export default plugin;
}
